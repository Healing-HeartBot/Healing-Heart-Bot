function findRandomByMood(mood) {
// takes in an array of objects
// 

}

export.module = findRandomByMood;


//count all the responses that match the mood
//then going to get a random number between 1 and count total
//find 'one' and do a .skip(random number we just got from function)
//